__pycache__/
*.pyc
.DS_Store
Thumbs.db

# Personal lore store (`personalStoreDir` in .lore.json) — per-person notes about
# developing this plugin, read by recall alongside the team store but never
# published. `learnings/` is deliberately NOT ignored here: the team store is
# committed and shared by default, and that default is what users of the plugin
# get.
.lore/
